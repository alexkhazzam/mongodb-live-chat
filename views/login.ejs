<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/css/login-register.css" />
    <script src="/js/login.js" defer></script>
    <%- include("./includes/bootstrap.ejs") %>
    <%- include("./includes/font-awesome.ejs") %>
</head>
<body>
    <form id="login-form" onsubmit="showLoadingSpinner()" action="/login" method="POST">
        <div id="logo-wrapper">
          <span>Chat live today!</span>
          <a href="https://nhsforeignaffairs.herokuapp.com" target="_blank">
            <img src="/images/registerPage/logo.png" width="75" height="75">
          </a>
        </div>
            <div class="form-group">
              <input type="email" class="form-control field" name="email" placeholder="Enter email" id="email" required value="<%= email %>">
                <small class="error-message" id="email-error" style="display: <%= display %> !important">Incorrect email and/or password.</small>
            </div>
            <div class="form-group">
                <input type="password" class="form-control field" name="password" placeholder="Password" id="password" required value="<%= pass %>">
                <small class="error-message" id="email-error" style="display: <%= display %> !important">Incorrect email and/or password.</small>
            </div>
            <div class="register-btn-wrapper">
                <button class="btn btn-primary" id="login-btn" type="submit">Login</button>
                <div class="spinner-border text-primary" id="loading-spinner" role="status">
                  <span class="sr-only">Loading...</span>
                </div>
            </div>
            <p id="link-to-register">Dont't have an account? Click <a href="/register">Here</a> to register.</p>
    </form>
</body>
</html>